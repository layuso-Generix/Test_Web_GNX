function esc(t){return String(t==null?"":t).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;");}
function inlineMd(text){return esc(text).replace(/\*\*(.+?)\*\*/g,"<strong>$1</strong>").replace(/`(.+?)`/g,'<code style="background:var(--gray-100);padding:1px 5px;border-radius:3px;font-family:monospace">$1</code>').replace(/\[([^\]]+)\]\(([^)]+)\)/g,'<a class="download-link" href="$2" target="_blank">$1</a>');}
function simpleMarkdown(md, skipFirstH=false){const lines=(md||"").split("\n");let html="",inUl=false,first=false,inCode=false,code=[];function closeUl(){if(inUl){html+="</ul>";inUl=false}}function closeCode(){if(inCode){html+=`<div class="code-panel"><pre class="code-pre">${esc(code.join("\n"))}</pre></div>`;inCode=false;code=[]}}for(const raw of lines){const line=raw.trimEnd();if(line.startsWith("```")){if(inCode)closeCode();else{closeUl();inCode=true;code=[]}continue;}if(inCode){code.push(raw);continue;}if(line.startsWith("### ")){closeUl();html+=`<h3>${esc(line.slice(4))}</h3>`}else if(line.startsWith("## ")||line.startsWith("# ")){closeUl();const text=line.startsWith("## ")?line.slice(3):line.slice(2);if(skipFirstH&&!first){first=true;html+=`<div style="border-bottom:2px solid var(--generix-orange-soft);margin:0 0 24px"></div>`}else html+=`<h2>${esc(text)}</h2>`}else if(line.startsWith("- ")||line.startsWith("* ")){if(!inUl){html+="<ul>";inUl=true}html+=`<li>${inlineMd(line.slice(2))}</li>`}else if(line.startsWith(">")){closeUl();html+=`<div class="highlight-box">${inlineMd(line.slice(1).trim())}</div>`}else if(line.trim()===""){closeUl()}else{closeUl();html+=`<p>${inlineMd(line)}</p>`}}closeUl();closeCode();return html;}
async function loadTextFile(path){const r=await fetch(path);if(!r.ok)throw new Error(`No se pudo cargar: ${path}`);return r.text();}
async function renderReadme(section, container){container.innerHTML=`<p style="color:var(--gray-500)">${t("loading")}</p>`;const path=section.readme&&section.readme[LANG()];if(!path){container.innerHTML=`<div class="info-box"><strong>${t("readme.missing")}</strong><br>${t("readme.missingHint")}</div>`;return;}try{const md=await loadTextFile(path);container.innerHTML=simpleMarkdown(md,true);}catch(e){container.innerHTML=`<div class="info-box"><strong>${t("readme.missing")}</strong><br>${t("readme.missingHint")}<br><code>${esc(path)}</code></div>`;}}
window.esc=esc;window.simpleMarkdown=simpleMarkdown;window.loadTextFile=loadTextFile;window.renderReadme=renderReadme;
